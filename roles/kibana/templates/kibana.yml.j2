server.host: "0.0.0.0"

elasticsearch.hosts: ["http://elastic:9200"]

# Kibana UI
server.port: 5601

# Logging
logging:
  appenders:
    default:
      type: console
      layout:
        type: json
  root:
    level: info
    appenders: [default]